<?xml version="1.0" encoding="UTF-8"?>
<biome:views xmlns:biome="http://github.com/mermetbt/Biome/">

	<biome:include src="elements/navbar.xml"/>

	<biome:view action="profile">
		<biome:title title="Profile"/>

		<div class="row">
			<div class="col-lg-12">
				<biome:panel title="User informations">
					<biome:form>
						<biome:field value="#{auth.user.firstname}"/>
						<biome:field value="#{auth.user.lastname}"/>
						<biome:field value="#{auth.user.mail}"/>
						<biome:button class="btn-success" value="Save" action="#{user.save}"/>
					</biome:form>
				</biome:panel>
				<biome:panel title="Password change">
					<biome:form>
						<biome:field type="password" value="#{auth.password}" label="Password" required="true"/>
						<biome:field type="password" value="#{auth.password_confirm}" label="Password Confirm" required="true"/>
						<biome:button class="btn-success" value="Change" action="#{user.update_password}"/>
					</biome:form>
				</biome:panel>
			</div>
		</div>
	</biome:view>

	<biome:view action="index">
		<biome:title title="Users">
			<biome:a class="btn btn-success" controller="user" action="create"><i class="fa fa-plus"></i><span> New</span></biome:a>
		</biome:title>

		<div class="row">
			<div class="col-lg-12">
				<biome:panel title="Users list">
					<biome:datatable value="#{users.users}" var="u">
						<biome:column headerTitle="#">
							<biome:variable value="#{u.user_id}"/>
						</biome:column>

						<biome:column headerTitle="Firstname">
							<biome:a controller="user" action="show" item="#{u.user_id}">
								<biome:variable value="#{u.firstname}"/>
							</biome:a>
						</biome:column>

						<biome:column headerTitle="Lastname">
							<biome:a controller="user" action="show" item="#{u.user_id}">
								<biome:variable value="#{u.lastname}"/>
							</biome:a>
						</biome:column>

						<biome:column headerTitle="E-Mail">
							<biome:variable value="#{u.mail}"/>
						</biome:column>
					</biome:datatable>

				</biome:panel>
			</div>
		</div>
	</biome:view>

	<biome:view action="create">
		<biome:title title="Add new user"/>

		<biome:panel title="User informations">
			<biome:form>
				<biome:field value="#{user.firstname}"/>
				<biome:field value="#{user.lastname}"/>
				<biome:field value="#{user.mail}"/>
				<biome:field value="#{user.password}"/>
				<biome:button class="btn-success" value="Create" action="#{user.create}"/>
			</biome:form>
		</biome:panel>
	</biome:view>

	<biome:view action="show">
		<biome:title title="#{user.firstname} #{user.lastname}">
			<biome:a class="btn btn-danger" controller="user" action="delete" item="#{user.user_id}"><i class="fa fa-trash"></i><span> Delete</span></biome:a>
		</biome:title>

		<div class="row">
			<div class="col-lg-8">
				<biome:panel title="User informations">
					<biome:ajaxfield value="#{user.firstname}"/>
					<biome:ajaxfield value="#{user.lastname}"/>
					<biome:ajaxfield value="#{user.mail}"/>
					<biome:ajaxfield value="#{user.user_active}"/>
					<biome:ajaxfield value="#{user.creation_date}"/>
				</biome:panel>
			</div>
			<div class="col-lg-4">
				<biome:panel title="Informations">

					<biome:datatable value="#{user.roles}" var="r" pagination="false" searchable="false">
						<biome:column headerTitle="Role">
							<biome:variable value="#{r.role_name}"/>
						</biome:column>
						<biome:column>
							<biome:a class="btn btn-xs btn-danger" controller="user" item="#{user.user_id}" action="removeRole" page="#{r.role_id}"><i class="fa fa-minus"></i></biome:a>
						</biome:column>
					</biome:datatable>

					<biome:form>
						<div class="row">
							<div class="col-lg-8">
								<biome:field label="" value="#{userrole.role_id}"/>
							</div>
							<div class="col-lg-4">
								<biome:button class="btn-sm btn-success" action="#{user.addrole}"><i class="fa fa-plus"></i><span> Add</span></biome:button>
							</div>
						</div>
					</biome:form>

				</biome:panel>
			</div>
		</div>
	</biome:view>

</biome:views>
